export enum ECode {
  SUCCESS = "200",
  ERROR = "500",
}

export enum EMsg {
  SUCCESS = "请求成功",
  ERROR = "服务器出错了",
  DEVICEEMPTY = "设备不能为空",
  DEVICENOBIND = "未绑定设备",
}

export enum EMethod {
  GET = "get",
  POST = "post",
}
